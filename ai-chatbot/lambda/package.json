{
  "name": "nakoba-ai-chatbot-lambda",
  "version": "2.0.0",
  "description": "Enterprise AI Chatbot Lambda — Nakoba Production",
  "main": "index.js",
  "scripts": {
    "test": "node test.js",
    "package": "npm install --production && zip -r ../lambda.zip . -x '*.zip' -x 'test.js' -x '*.test.js'",
    "audit": "npm audit --audit-level=high"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-secrets-manager": "^3.600.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}
